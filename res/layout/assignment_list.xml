<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AssignmentList" >

    <Button
        android:id="@+id/assignment_add_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:onClick="addAssignment"
        android:text="Add Assignment" />

    <Button
        android:id="@+id/assignment_remove_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/assignment_add_button"
        android:layout_marginTop="20dp"
        android:onClick="removeAssignment"
        android:text="Remove Assignment" />

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/refresh_assignment_button"
        android:layout_marginTop="20dp" >
    </ListView>

    <Button
        android:id="@+id/refresh_assignment_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/assignment_remove_button"
        android:onClick="refreshAssignmentList"
        android:layout_marginTop="20dp"
        android:text="Refresh assignment list" />

</RelativeLayout>