<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/texture"
    tools:context=".MainActivity" >

    <ListView
        android:id="@+id/c_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:divider="@android:color/transparent"
        android:dividerHeight="10.0sp"
        android:layout_marginBottom="60dp" >
    </ListView>
    
   <!-- <ImageButton
        android:id="@+id/course_remove_button"
        android:contentDescription="Removes course"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/remove_button"
        android:onClick="removeCourse"
        android:text="Remove Course" />-->

     <ImageButton
         android:id="@+id/course_add_button"
         android:contentDescription="Adds course"
         android:layout_width="53dp"
         android:layout_height="53dp"
         android:layout_alignLeft="@android:id/list"
         android:layout_alignParentBottom="true"
         android:background="@drawable/add_button"
         android:layout_alignParentLeft="true"
         android:onClick="addCourse"
         android:text="Add Course" />

     <!--<ImageButton
         android:id="@+id/refresh_course_button"
         android:contentDescription="Refreshes course list"
         android:layout_width="50dp"
         android:layout_height="50dp"
         android:layout_alignParentBottom="true"
         android:layout_alignParentRight="true"
         android:background="@drawable/refresh_button"
         android:onClick="refreshCourseList"
         android:text="Refresh course list" />-->

</RelativeLayout>