<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SubTask_List" >

    <Button
        android:id="@+id/subtask_add_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:onClick="addsubtask"
        android:text="Add subtask" />

    <Button
        android:id="@+id/subtask_remove_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/subtask_add_button"
        android:layout_marginTop="20dp"
        android:onClick="removesubtask"
        android:text="Remove subtask" />

    <ListView
        android:id="@+id/task_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/refresh_subtask_button"
        android:layout_marginTop="20dp" >
    </ListView>

    <Button
        android:id="@+id/refresh_subtask_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/subtask_remove_button"
        android:onClick="refreshsubtaskList"
        android:layout_marginTop="20dp"
        android:text="Refresh subtask list" />

</RelativeLayout>